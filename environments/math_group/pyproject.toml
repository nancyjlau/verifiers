[project]
name = "math-group"
version = "0.1.1"
dependencies = [
    "verifiers>=0.1.8",
    "math-verify>=0.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["math_group.py"]
